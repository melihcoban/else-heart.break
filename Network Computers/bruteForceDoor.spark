# Add this to computer called "Lodge_Room1_PillarComputer_PillarComputer_1"

Print("RainOS Brute-Force Door")

number id = -1
#var connectionName = Input("Target Door: ")
string bruteDoor(string connectionName)
    loop x from 1 to 99999
        if x == Unlock(connectionName)
        else
        x = x+1
        Print(x)
    end
end
end
